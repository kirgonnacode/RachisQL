# Конфигурация Wren-проекта.
# Полная схема полей: https://docs.getwren.ai/oss/reference/mdl

# Создание Wren-проекта:
# docker compose exec backend bash
# cd /app/wren
# wren context init --force

# После заполнения всех файлов:
# wren context validate
# wren context build

schema_version: 5
name: RachisQL
catalog: wren
schema: public
data_source: postgres